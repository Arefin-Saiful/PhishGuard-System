# ───────── core Flask stack ─────────
Flask==2.3.2
Flask-Login==0.6.3
Flask-SQLAlchemy==3.1.1
Flask-Mail==0.9.1
Werkzeug==2.3.7
itsdangerous==2.1.2
Jinja2==3.1.3
click==8.1.7
SQLAlchemy==2.0.29              # explicit to match warning

# ───────── ML / model-loading ──────
# TensorFlow pulls in Keras; if you are on CPU-only, use tensorflow-cpu instead.
tensorflow==2.16.1
numpy>=1.25,<2.0
scikit-learn==1.5.0
joblib==1.5.1

# ───────── utilities ───────────────
python-dotenv==1.0.1            # loads secrets from .env
Pillow>=10.0.0                  # if you ever need to manipulate images (optional)

# ───────── production server (optional) ────────
gunicorn==21.2.0
